import React from "react";
import { Header, navbarVE } from "../../styles/Layout/Navbar.css";

const Navbar = () => {
  return (
    <section className={navbarVE}>
      <h1 className={Header}>Hello container</h1>
    </section>
  );
};

export default Navbar;
